import React from "react";

type BoxTextProps = {
  text: string;
};

/**
 * A BoxText component is a piece of text surrounded by a colored box
 */
const BoxText = ({ text }: BoxTextProps) => {
  return <>Hello there</>;
};

export default BoxText;
